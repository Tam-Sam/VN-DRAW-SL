<!--
	The map itself is rendered by the root layout (so it stays mounted across tab
	switches). This route intentionally has no content of its own — its panels
	(IndicatorPanel / InfoPanel) are rendered by +layout.svelte when this route is active.
-->
