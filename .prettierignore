# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Legacy qgis2web export + the user's own source data — never reformat these,
# they are read directly by src/routes/api and must stay byte-identical to source.
/css/
/data/
/downloads/
/js/
/legend/
/webfonts/
/SUPERSEDED/
/index.html
